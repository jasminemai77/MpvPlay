name: platform_bridge
version: 0.1.0
publish_to: none
environment:
  sdk: ^3.12.0
resolution: workspace
dependencies:
  path: ^1.9.0
  playback_runtime: any
  playback_protocol: any
  player_core: any

dev_dependencies:
  test: ^1.25.0
