# v0.1 Scope

Windows-only core playback loop: select local audio files, load a temporary queue, control playback, show/seek position, adjust volume/mute, advance on completion, report recoverable errors, and persist/restore a basic session. Android, media library/database, metadata, lyrics, video and DSP remain out of scope.

