# v0.4 scope

Runtime-owned playback queue management: UUIDv7 entry identities, repeat,
shuffle, editing, JSON session restoration, and Windows UI controls. Drift
schema remains v3; media-library and playback runtime remain independent.
