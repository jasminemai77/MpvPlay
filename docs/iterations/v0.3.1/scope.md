# v0.3.1 Playback History Scope

This iteration delivers a single local user loop: start a library track,
persist the start event, browse recent history and cumulative play counts,
replay available entries, and clear history. It excludes shuffle, repeat,
queue editing, recommendations, time-threshold play rules, cloud sync, and
Android.
