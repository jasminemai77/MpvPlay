# v0.3.1 Known Issues

- A play counts at actual playback start, not after a duration threshold; this
  is the explicit product rule for this release.
- History has no per-entry deletion, search, analytics, shuffle, repeat, or
  cloud synchronization by design.
- The Version Gate remains `NOT_RUN` until full regression, remote CI, Release
  artifact, and Draft PR evidence exist.
